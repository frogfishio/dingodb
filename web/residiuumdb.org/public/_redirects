# Apex is canonical; configure www.residiuumdb.org → apex at the DNS/CDN layer as a 301.
# HTTP → HTTPS is handled by the host.
#
# Principal ops (outside this file): point dingodb.org / docs.dingodb.org
# DNS/CDN at Residiuum hosts when cutover is authorized.

# Docs site (canonical host docs.residiuumdb.org)
/docs/*  https://docs.residiuumdb.org/:splat  302
/get-started/*  https://docs.residiuumdb.org/getting-started/rust/  302
