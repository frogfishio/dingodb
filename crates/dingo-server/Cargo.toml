[package]
name = "dingo-server"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license = "AGPL-3.0-or-later"
authors.workspace = true
description = "DingoDB networked server: accept loop, authz, admission, Raft RPC glue (AGPL)."
readme = "README.md"
repository.workspace = true
keywords = ["dingodb", "server", "raft", "rpc"]
categories = ["database", "network-programming"]
include = ["Cargo.toml", "README.md", "src/**"]

[dependencies]
dingo-sdk = { workspace = true, features = ["cluster"] }
dingo-store.workspace = true
dingo-cluster.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
rustls = { version = "0.23", default-features = false, features = ["ring", "std", "tls12"] }
rustls-pemfile = "2"
rustls-pki-types = "1"

[dev-dependencies]
tempfile.workspace = true
