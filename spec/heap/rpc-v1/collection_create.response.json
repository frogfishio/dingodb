{
  "type": "object",
  "additionalProperties": false,
  "description": "Result for op 106 collection_create (CORE plan §6.2).",
  "properties": {
    "collection_id": {
      "type": "string",
      "minLength": 36,
      "maxLength": 36
    },
    "canonical_name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256
    },
    "descriptor_hash": {
      "type": "string",
      "minLength": 64,
      "maxLength": 64,
      "description": "32-byte BLAKE3 (or profile hash) as 64 lowercase hex characters."
    },
    "receipt": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "receipt_id": {
          "type": "string",
          "minLength": 32,
          "maxLength": 32
        },
        "operation": {
          "type": "string",
          "const": "create_collection"
        },
        "heap_id": {
          "type": "string",
          "minLength": 36,
          "maxLength": 36
        },
        "object_id": {
          "type": "string",
          "minLength": 36,
          "maxLength": 36
        },
        "descriptor_hash": {
          "type": "string",
          "minLength": 64,
          "maxLength": 64
        },
        "created_at": {
          "type": "integer",
          "minimum": 0,
          "description": "Unix seconds (UTC)."
        }
      },
      "required": [
        "receipt_id",
        "operation",
        "heap_id",
        "object_id",
        "descriptor_hash",
        "created_at"
      ]
    }
  },
  "required": [
    "collection_id",
    "canonical_name",
    "descriptor_hash",
    "receipt"
  ]
}
