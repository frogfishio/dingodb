//! Verus placeholder — pure kernel proofs land with Gate H6.
//!
//! HP-001 delivers the executable Rust decision function; this crate path is
//! reserved so `scripts/verify-heap.sh` has a stable location.

#![allow(dead_code)]

/// Marker that the Verus project path exists.
pub fn heap_verus_scaffold() -> bool {
    true
}
