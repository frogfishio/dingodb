# Residuum Rules specification (renamed)

The language has been renamed **Residuum Rule Expression (RRE)**.

The normative specification is [RRE_SPEC.md](RRE_SPEC.md). The source dialect
identifier is `dre`.
