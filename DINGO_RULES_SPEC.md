# Dingo Rules specification (renamed)

The language has been renamed **Dingo Rule Expression (DRE)**.

The normative specification is [DRE_SPEC.md](DRE_SPEC.md). The source dialect
identifier is `dre`.
